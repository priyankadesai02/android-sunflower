pipeline {
         agent any
         stages {
                 stage('build') {
				 steps {
					sh "./gradlew clean assemble"
                     echo 'Hi, this is Priyanka'
					}
								}
                 
                 }
		}
                 